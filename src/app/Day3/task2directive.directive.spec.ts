import { Task2directiveDirective } from './task2directive.directive';

describe('Task2directiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Task2directiveDirective();
    expect(directive).toBeTruthy();
  });
});
