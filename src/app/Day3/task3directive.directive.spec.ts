import { Task3directiveDirective } from './task3directive.directive';

describe('Task3directiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Task3directiveDirective();
    expect(directive).toBeTruthy();
  });
});
